# Core GUI framework
PyQt5>=5.12.0
PyQt5-tools>=5.12.0.1.3

# YAML processing for configuration files
PyYAML>=5.4.0

# Scientific computing
numpy>=1.19.0
matplotlib>=3.3.0

# Utility libraries
psutil>=5.7.0
pexpect>=4.8.0

# Network and system utilities (Python packages only)
# For system dependencies, see installation instructions in README.md

# System dependencies that need to be installed via package manager:
# 
# REQUIRED for basic functionality:
# sudo apt-get install python3 python3-pip python3-tk python3-pyqt5
# 
# REQUIRED for Docker-based 5G simulation:
# sudo apt-get install docker.io docker-compose
# sudo usermod -aG docker $USER  # (logout/login required)
# 
# REQUIRED for wireless simulation:
# sudo apt-get install wireless-tools rfkill iw
# sudo modprobe mac80211_hwsim radios=10
# 
# OPTIONAL for native Mininet installation:
# sudo apt-get install mininet python3-mininet
# 
# For Mininet-WiFi and Containernet, use the provided Docker container:
# cd netflux5g-editor/src/automation/mininet
# docker build -t mn-wifi:v1 .
# 
# Or install manually following these projects:
# - Mininet-WiFi: https://github.com/intrig-unicamp/mininet-wifi
# - Containernet: https://github.com/containernet/containernet
#
# To check what's installed:
# python3 -c "from prerequisites.checker import PrerequisitesChecker; print(PrerequisitesChecker.check_all_prerequisites())"
